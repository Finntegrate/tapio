# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Vendored agent-tooling skill/reference docs added by `sv create` scaffolding
# (Claude, Gemini, GitHub Copilot). Copied verbatim from upstream -- not
# authored here, so we don't reformat them.
/.github/skills/
/.github/agents/
/.gemini/

# inlang/Paraglide-generated, gitignored, and regenerated by `vite dev`/`build`
# while it's running -- never meant to be formatted.
/project.inlang/.meta.json
/project.inlang/README.md
/project.inlang/cache/
