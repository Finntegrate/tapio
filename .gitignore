# OS
.DS_Store

# Environments
.env
.env.*
!.env.example
!.env.test

# Shared pipeline data (crawler -> ingest -> tapio), not owned by one service
/content/
/vectorstore/
chroma_db/
results.json
crawl4ai/
crawl/
